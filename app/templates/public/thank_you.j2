{% extends "base.j2" %}
{% from "macros/form.j2" import input_form_6, input_form_12 %}
{% set page = "thank_you" %}

{% block content %}
<div class="container-xxl py-6 wow fadeInUp" data-wow-delay="0.1s">
  <div class="container text-center">
    <div class="row justify-content-center">
      <div class="col-lg-6">
        <i class="bi bi-heart display-1 text-danger"></i>
        <h1 class="mb-4">Спасибо!</h1>
        <p class="mb-4">Мы получили вашу заявку и свяжемся с вами в ближайшее время.</p>
        <div class="error-actions d-flex flex-wrap gap-2 justify-content-center">
            <a href="{{ url_for('core.index') }}" class="btn btn-primary py-sm-3 px-sm-5">Вернуться на главную</a>
            <a href="{{ url_for('core.page', page_name='contacts') }}" class="btn btn-light py-sm-3 px-sm-5 ms-3">Связаться с нами</a>
        </div>
        <div hidden>{{ thank_you }}</div>
      </div>
    </div>
  </div>
</div>
{% endblock content %}
